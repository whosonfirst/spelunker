package server

import (
	"testing"
)

func TestCloneOptions(t *testing.T) {

	/*

		slog.Info("1a", "foo", opts.Foo)
		slog.Info("1b", "foo", run_options.Foo)

		opts.Foo = "Alice"
		slog.Info("2a", "foo", opts.Foo)
		slog.Info("2b", "foo", run_options.Foo)

		run_options.Foo = "Doug"
		slog.Info("3a", "foo", opts.Foo)
		slog.Info("3b", "foo", run_options.Foo)

	*/

	t.Skip()
}
